/**
 * Brevo configuration — copy to email-config.js and set your API URL.
 * Deploy api/send-otp.js to Vercel first (see EMAIL-SETUP.md).
 */
const RAVEN_EMAIL_CONFIG = {
  enabled: true,
  apiUrl: 'https://YOUR_VERCEL_PROJECT.vercel.app/api/send-otp',
};
