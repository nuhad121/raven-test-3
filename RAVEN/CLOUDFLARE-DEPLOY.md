# Moved — use this guide instead

**[CLOUDFLARE-SETUP.md](./CLOUDFLARE-SETUP.md)** — full Bangla + English setup for ahnuhad.site

Quick deploy:

```powershell
cd "C:\Users\AH NUHSAD\RAVEN"
npm install
npm run deploy
```

Or push to GitHub → Actions → **Deploy to Cloudflare Pages**.
